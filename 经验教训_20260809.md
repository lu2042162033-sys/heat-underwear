# 经验教训记录

> 日期：2026-08-09（Argentina 时区）· 项目：HEAT UNDERWEAR 商品展示网页（GitHub Pages 迁移 + 管理面板）

## 一、当日大事记

1. **Railway → GitHub Pages 迁移**：因 Railway 免费 30 天到期不可续，迁移到永久免费的 GitHub Pages；商品数据从磁盘卷改为仓库内 `products.json`，管理写入通过 GitHub Actions（口令存 Secret，前端只存令牌于浏览器）。
2. **排障马拉松**：管理面板登录一直提示“口令不正确”，从晚上 19:00 排查到 19:40，最终定位为 HTTP 204 误判。
3. 最终结果：全链路验证通过（登录 → 增删改 → 自动部署 → 线上更新）。

## 二、技术教训

### 1. HTTP 204 = 成功，但最容易踩坑

- GitHub `POST /repos/{owner}/{repo}/dispatches` 成功返回 **204 No Content**（无响应体）。
- 两个隐藏坑：`res.ok` 对 204 是 `false`；`res.json()` 解析空响应体会抛异常。
- **教训**：判断成功要用 `status >= 200 && status < 300`，并对 204/空响应体做专门处理；不要无条件 `res.json()`。
- **提前验证**：接到“某请求总失败”的报告，先查该接口的真实成功状态码与响应体，再写前端判断。

### 2. PowerShell 字符串替换是“静默失败”重灾区

- `.Replace()` 锚点不匹配时**不报错**，直接原样返回；导致多次“以为改好了，实际没改”。
- 本次真实事故：setDebug 等代码因锚点漂移从未写入文件，只有版本号被更新——线上显示 v6 但代码是旧版。
- **教训**：每次替换后必须**读回文件校验**关键字符串存在；批量改 JS 优先用 Node 脚本（`includes` 检查 + 明确报错）。

### 3. git rebase 与“机器人提交”会悄悄覆盖你的修改

- GitHub Actions 自动提交（`chore: admin update`）与本地的 `pull --rebase` 配合时，工作区编辑可能被回退/丢失。
- **教训**：本地改完立即 `git add + commit`；推送前检查 `git status`；rebase 后用 `git log` 确认自己的提交还在。

### 4. 浏览器排障要用真实浏览器，别靠截图来回传

- 用户截图（OCR 只能出 650×35 的细条）传递信息极有限；来回确认浪费大量时间。
- **教训**：用 Edge headless + CDP（Chrome DevTools Protocol）直接在真实浏览器里模拟点击、填表、抓取 DOM 状态与控制台，几分钟就能拿到决定性证据。
- 本次正是靠 CDP 复现看到 `请求发送失败（状态 204）` 才一击命中。

### 5. 追踪错误信息到源头，不要停留在“表象”

- “口令不正确”实际有 4 个可能来源：口令错、令牌错、请求失败、超时。最初统一显示同一文案，导致误诊。
- **教训**：用户可见的错误信息应能区分失败类别；排查时先问“这行提示是从哪段代码弹出来的”，再顺藤摸瓜。

### 6. GitHub Pages / 静态托管的现实约束

- dispatch 的 `client_payload` 上限 10KB → 大数据走“仓库内暂存文件 + 工作流读取”（pending-products.json 方案）。
- 细粒度 PAT 会被 push protection 拦截，不能进公开仓库代码；令牌只能存在浏览器/Secret。
- `raw.githubusercontent.com` CDN 缓存严重滞后（实测 6 分钟+）→ 轮询必须带时间戳穿透，且优先轮询站点同源地址。
- Pages 部署约 20-30 秒，管理操作后要留出等待时间。

## 三、流程教训

1. **改完必须验证**：每次文件修改后立刻读回校验；每次部署后立刻请求线上资源确认新版本。
2. **复杂问题先搭可复现环境**：能自动复现就不要手工猜。
3. **及时清理调试代码**：功能确认后要把临时的调试输出收敛/移除，避免交付噪音。
4. **遇到“用户说不行但我测试没问题”**：第一反应应该是“用户环境与我环境哪里不同”，而不是反复加日志。

## 四、本项目的关键运维信息（备忘）

- 线上地址：https://lu2042162033-sys.github.io/heat-underwear/
- 仓库：https://github.com/lu2042162033-sys/heat-underwear（公开）
- 管理口令：`sarmiento2716`（存在 GitHub Actions Secret `ADMIN_PASSWORD`，代码中不出现）
- 管理令牌：管理员浏览器里粘贴的 Fine-grained PAT（仅本仓库 Contents 读写），不存代码
- 数据文件：`w/outputs/site/data/products.json`；管理写入经 `update-products.yml` 校验口令后提交并触发部署
- 本地备份：`w/outputs/backup/products_backup_20260809.json`（192 条商品）

### 7. GitHub Contents API 更新已存在文件必须带 sha（422 陷阱）

- 管理面板"保存商品"失败根因：仓库里已有 `pending-products.json`，Contents API 的 PUT 不带原文件 `sha` 时返回 **422**。
- **教训**：GitHub Contents API 更新已有文件前必须先 GET 拿 `sha`，再随 PUT 提交；创建新文件才可不带。
- 排查套路复用：给保存流程加逐步调试（pending 写入状态 → dispatch → 回执），一次复现就锁定 422。
- 真实浏览器（CDP）完整走通"登录→新增→保存→部署→线上可见→清理"，比手工测试可靠得多。

### 8. raw.githubusercontent.com 缓存滞后影响的不只是回执，还有商品数据

- 商品列表 fetch 用 raw 地址时，保存后刷新看到的仍是旧数据（raw 缓存滞后 6 分钟+）。
- **教训**：GitHub Pages 站点上所有"实时数据"读取都应**站点同源优先 + raw 兜底**，并带时间戳穿透缓存。
- **本地存储策略**：localStorage 只作离线兜底；线上数据应始终权威，fetch 成功后覆盖本地缓存并重渲染。

### 9. 管理后台的"保持登录"与发布反馈

- 登录令牌/口令存浏览器 localStorage，刷新后自动恢复登录态并直接打开管理面板（登出时清除）。
- 保存成功提示应说明"正在发布，约 30-60 秒生效"，避免用户刷新太早以为数据丢失。
- 保存流程的真实耗时：pending 写入（1-2s）+ 工作流提交（10-15s）+ Pages 部署（20-25s）≈ 30-40 秒，这是 GitHub Pages 架构的固有延迟，需在文案中引导用户等待。

### 10. 静态站功能扩展（分类菜单 / 购物车 / PDF）要点

- **大改 JS 的教训**：分多批字符串替换时，批次间锚点会互相影响，且 PowerShell 对 `$`、引号有特殊解释（`'$'` 会截断字符串）。批量生成代码优先用独立 Node 脚本文件（apply_patch 写入），用 String.fromCharCode 构造特殊字符，或逐行数组拼接后写文件，避免 shell 转义问题。
- **DOM 渲染中断排查**：`getElementById` 找不到元素（如 class 写成 id）会导致渲染函数静默失败（items 0）。HTML 元素约定要一致：被 JS 查询的元素必须有对应 id。
- **购物车/PDF 方案**：localStorage 持久化 + window.open 打印版订单页 + window.print()，零依赖、离线可用、中文可靠；按件/按打计价用成打价×(数量/12)。
- **数据迁移**：改分类时同步改 products.json（线上数据）与 products.js（种子），两者不一致会导致刷新后数据回退。
- **验证方式**：Edge headless + CDP 分步测试（菜单筛选、购物车增减、PDF 内容拦截验证），比手工点击可靠。

